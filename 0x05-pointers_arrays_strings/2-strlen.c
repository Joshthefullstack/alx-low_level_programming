#include "main.h"
#include <string.h>

/**
 * a function that calculates the length of a string *s
 * using strlen function
 * Return: return 0
 */
void _strlen(char *s)
{
	size_t strlen(const char *s)
}
