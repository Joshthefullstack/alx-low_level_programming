#include  "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * int _islower(int c) -  to return 1 if c is lowercase
 *
 * Return: Always 0.
 */
int islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	putchar('\n');
}
